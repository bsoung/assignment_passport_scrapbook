
{{#if profileInfo}}
	<h2>Provider: {{profileInfo.provider}}</h2>
	<h3>Email: {{profileInfo.email}}</h3>
	<h3>Name: {{profileInfo.name}}</h3>
{{!-- 	{{#if profile.photos.length}}
		{{#each profile.photos as |photoUrl|}}
			<img src="{{photoUrl}}" />
		{{/each}}
	{{/if}} --}}
{{/if}}
